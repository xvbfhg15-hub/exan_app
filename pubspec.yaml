name: exam_app
description: تطبيق اختبارات تفاعلي مع تحقق ذكاء اصطناعي
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  file_picker: ^6.1.1
  syncfusion_flutter_pdf: ^24.1.41
  signature: ^5.4.0
  image_picker: ^1.0.7
  http: ^1.2.0
  dio: ^5.4.1
  provider: ^6.1.2
  path_provider: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
